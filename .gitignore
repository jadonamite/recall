node_modules/
logs/
.env
.hydradb/

# The crawled dataset IS committed — it makes the repo runnable without waiting
# on a deps.dev crawl and a few thousand OSV queries. Only derived state is not:
data/idmap.json

# static demo build output (deployed to Vercel, not source)
dist/
.vercel/

# Next.js site build
site-next/node_modules/
site-next/.next/
site-next/out/
site-next/public/site.json

# font/logo build inputs — the outputs are embedded or committed
.fonts/
.assets/
